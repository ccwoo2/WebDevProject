import React from "react";

const SearchBar = () => {
  return (
    <div>
      <input type="text" placeholder="Search for NBA Players" />
      <button>Search for NBA Players</button>
    </div>
  );
};

export default SearchBar;
